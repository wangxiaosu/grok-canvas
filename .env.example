# 默认无需环境变量。按需将此文件复制为 .env.local，再取消对应注释。
# 使用绝对路径；环境变量里的 ~ 不会自动展开。Windows 可用 C:/Users/you/...
# 画布、素材、生成记录和视频任务默认保存在用户目录的 .grok-canvas/data 下。
# GROK_CANVAS_DATA_DIR=/absolute/path/to/grok-canvas/data

# OAuth 凭据默认保存在用户目录的 .grok-canvas/auth.json。
# GROK_CANVAS_AUTH_PATH=/absolute/path/to/grok-canvas/auth.json

# 只支持本机 3210 端口，通常不要修改回调地址。
# XAI_REDIRECT_URI=http://127.0.0.1:3210/callback
